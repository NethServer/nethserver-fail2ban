{
return "\n#recidive not used on this server"
           unless (-f '/var/log/fail2ban.log');

my $status   = $fail2ban{Perpetual_status} || 'true';

$OUT .= qq (
[recidive]
enabled   = $status
logpath   = /var/log/fail2ban.log
maxretry  = 3
bantime   = -1
findtime  = 2592000
);
}
