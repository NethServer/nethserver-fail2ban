{
my $sieve = $dovecot{status} || 'disabled';
return "#sieve not used on this server" unless ($sieve eq 'enabled');
}

[sieve]
enabled  = {($fail2ban{Sieve_status} || 'true');}
port     = {($postfix{TCPPorts} || '25,465,587');}
logpath  = %(dovecot_log)s
